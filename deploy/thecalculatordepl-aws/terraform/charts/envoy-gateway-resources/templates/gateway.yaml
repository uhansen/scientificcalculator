apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ .Values.envoyGateway.gatewayName }}
  namespace: {{ .Values.app.namespace }}
spec:
  gatewayClassName: {{ .Values.envoyGateway.className }}
  listeners:
    - name: http
      protocol: HTTP
      port: 80
