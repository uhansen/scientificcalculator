{
  "name": "moddiv",
  "version": "0.1.0",
  "type": "module",
  "description": "WebAssembly component implementing mod (remainder) and div (quotient)",
  "scripts": {
    "types": "jco types wit/world.wit -o types",
    "build:ts": "tsc",
    "componentize": "jco componentize --wit wit/world.wit --world-name moddiv-world --out moddiv.wasm --disable=all moddiv.js",
    "build": "npm run build:ts && npm run componentize"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "^1.19.0",
    "typescript": "^5.8.3"
  }
}
